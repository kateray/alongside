= render "layouts/nav"
#timeline

.extra_space
  - if current_user && current_user == @user
    .big_text
%input#init-data{type: 'hidden', value: raw(@initData.to_s)}

.zoom_button{data: {zoom: 'in'}} +
.zoom_button{data: {zoom: 'out'}} –


.message#private_message
  This page is now private
.message#public_message
  This page is now public. Share it!
  = link_to image_tag('tweetbutton.png'), @tweet_url, :target => "_blank"

%script{:type => "text/template", :id => "hovercardTemplate"}
  .place
    <%= venue_name %>
  .date
    <%= date %>
    <%= time %>
  <% if (shout) { %>
  .shout
    "<%= shout %>"
  <% } %>
  .friends
    <%= friends %>